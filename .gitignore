**/reviews/
**.vscode/
**/.claude/
CLAUDE.md

# Local Claude Code settings (not shared)
.claude/settings.local.json

# Deploy outputs at repo root — generated by deploy.sh from core/.
# Source templates live in core/; root-level paths are regenerated on each
# deploy run and must never be committed. Leading slash anchors these to the
# repo root so core/<same name> is never affected.
/AGENTS.md
/.context/
/.cursor/
/.devin/
/.windsurfrules
/.github/copilot-instructions.md
/.github/skills/

# Assessment and review outputs landing at the repo root
# (e.g. pxsweb-domain-security-assessment.md). These are engagement artefacts,
# not source, and must not be contributed back to this template repo.
/*-assessment.md
/*-review.md
/*-pen-test.md
/*-security-review.md
